<UserControl x:Class="NuGet.Tools.PackageSummaryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Image Height="32" Width="32" Stretch="Fill"  x:Name="_icon"></Image>
            <TextBlock x:Name="_id" FontSize="20" TextWrapping="Wrap" Margin="4,0,0,0">package id</TextBlock>
        </StackPanel>        
        <TextBlock x:Name="_tags" Grid.Row="1" TextWrapping="Wrap">Tags: </TextBlock>
        <TextBlock x:Name="_summary" Grid.Row="2" TextWrapping="Wrap">Summary </TextBlock>
    </Grid>
</UserControl>
